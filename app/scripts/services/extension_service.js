angular.module('kalmetApp.services')
.factory('extension', [function() {
  'use strict';

  return {

    /**
     * Modifies the lists of checks that are performed on each line.
     */
    extendConstraints: function(initialConstraints, constraints, otherConstraints) {

    },

    /**
     * Modifies the collections of UI text strings and CSS styles.
     */
    extendUi: function(txt, buttonStyle, messageStyle, iconStyle, textStyle) {

    }
  };
}]);